---
title: "LocusZoom.js"
output:
  html_document:
    self_contained: false
---

# JavaScript

```{r, results='asis', echo=FALSE}
library(htmltools)
lz <- tags$iframe(src ="lz.htm", width = "100%", height = "800px")
print(lz)
```

# Shiny

```{r, echo=FALSE}
library(shiny)

ui <- fluidPage(
  titlePanel("Protein-pQTL"),
  sidebarLayout(
    sidebarPanel(
      selectInput("imageSelect", "Protein-pQTL pair:",
                  choices = c(list("A1BG", "rs145685027", "19:58948122", "0"), list("ACE", "rs4353", "17:61570422", "1")),
                  selected = list("A1BG", "rs145685027", "19:58948122", "0"))
    ),
    mainPanel(
      imageOutput("selectedImage")
    )
  )
)

server <- function(input, output) {
  output$selectedImage <- renderImage({
    image_urls <- c(
      "https://example.com/image1.jpg",
      "https://example.com/image2.jpg"
    )
    selected_image_url <- image_urls[match(input$imageSelect, c(list("A1BG", "rs145685027", "19:58948122", "0"), list("ACE", "rs4353", "17:61570422", "1")))]
    list(src = selected_image_url, alt = "Selected Image")
  }, deleteFile = FALSE)
}

shinyApp(ui, server)
```
